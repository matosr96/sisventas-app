# Panel servido por nginx apuntando a una API ya levantada (la de sisventas-api en el puerto 8080).
services:
  app:
    build: .
    ports:
      - "4300:80"
    environment:
      API_URL: ${API_URL:-http://localhost:8080/api/v1}
    restart: unless-stopped
